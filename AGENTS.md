# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

**Product features**: see [docs/PRODUCT_SPEC.md](docs/PRODUCT_SPEC.md) for the full feature registry and implementation status.

**변경 이력**: 기능·정책을 바꾸면 `docs/changelog/releases.json`을 고치고 `npm run docs:changelog`로 `docs/CHANGELOG.pdf`를 다시 만든다. 규칙은 `.cursor/rules/changelog.mdc`.
